[build]
target-dir = "target"

[term]
verbose = false

[net]
git-fetch-with-cli = true

# Ship the Windows executable without a separately installed MSVC runtime.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
































