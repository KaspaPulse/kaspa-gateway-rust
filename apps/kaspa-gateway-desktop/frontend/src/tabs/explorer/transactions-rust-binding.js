/*
============================================================================
KGW_FINAL_FRONTEND_TRANSACTION_VIEW_ONLY
Frontend view/controller only.
This file may render progress, grouped days, collapsed rows, and UI state.
It must not implement API pagination, DB writes, forced-fetch deletion, or
transaction sync ownership. Transaction ownership belongs to Rust runtime.
============================================================================
*/

/*
 * KGW FRONTEND BINDING OWNER MAP
 * Generated by automated light cleanup script.
 * Role: Frontend transaction binding
 * Owner status: BINDING_ONLY
 *
 * Rules:
 * - Frontend code must call a small, explicit Tauri command surface.
 * - Transaction fetch/sync ownership lives in Rust runtime transaction_sync.rs.
 * - This file must not implement business rules that belong to the Rust runtime.
 * - No shell execution, no secrets, and no hidden fallback fetch path.
 */
export function installRustTransactionBinding() {
  window.kgwRunRustExplorerTransactions = null;
  window.kgwFetchAndRenderTransactions = null;
  window.kgwRenderGroupedTransactions = null;
  console.info("[KGW Transactions] external binding disabled; explorer.js owns fetch/render.");
}

installRustTransactionBinding();
